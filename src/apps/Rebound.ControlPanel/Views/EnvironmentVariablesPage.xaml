<Page
    x:Class="Rebound.ControlPanel.Views.EnvironmentVariablesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:behaviors="using:CommunityToolkit.WinUI.Behaviors"
    xmlns:converters="using:CommunityToolkit.WinUI.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:interactivity="using:Microsoft.Xaml.Interactivity"
    xmlns:local="using:Rebound.ControlPanel.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    xmlns:toolkit="using:CommunityToolkit.WinUI.Controls"
    xmlns:viewModels="using:Rebound.ControlPanel.ViewModels"
    mc:Ignorable="d">
    <Page.Resources>
        <converters:BoolToVisibilityConverter
            x:Key="BoolToInverseVisibilityConverter"
            FalseValue="Visible"
            TrueValue="Collapsed" />
    </Page.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid VerticalAlignment="Top" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
            <Image
                Width="16"
                Margin="16"
                HorizontalAlignment="Left"
                VerticalAlignment="Top"
                Source="/Assets/Glyphs/EnvironmentVariables.ico" />
            <TextBlock
                Margin="48,10,16,0"
                FontSize="18"
                FontWeight="SemiBold"
                Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"
                Text="Environment Variables"
                TextTrimming="CharacterEllipsis" />
            <TextBlock
                Margin="48,36,16,12"
                FontSize="12"
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                Text="Change environment variables for the current user or all users."
                TextWrapping="WrapWholeWords" />
        </Grid>
        <ScrollViewer Grid.Row="1">
            <StackPanel
                MaxWidth="1000"
                Margin="40"
                Spacing="8">
                <InfoBar
                    Title="Hover over a variable's value to view its full contents."
                    IsClosable="False"
                    IsOpen="True" />
                <TextBlock FontWeight="SemiBold" Text="User variables" />
                <Grid
                    Height="240"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="4">
                    <ScrollViewer Padding="4">
                        <ListView ItemsSource="{x:Bind ViewModel.UserVariables}" SelectedIndex="{x:Bind ViewModel.SelectedUserVariable, Mode=TwoWay}">
                            <ListView.Header>
                                <toolkit:DataTable Margin="12,4,0,8">
                                    <toolkit:DataColumn
                                        MinWidth="80"
                                        CanResize="True"
                                        Content="Name"
                                        DesiredWidth="100" />
                                    <toolkit:DataColumn
                                        MinWidth="160"
                                        CanResize="True"
                                        Content="Value"
                                        DesiredWidth="320" />
                                </toolkit:DataTable>
                            </ListView.Header>
                            <ListView.ItemTemplate>
                                <DataTemplate x:DataType="viewModels:EnvironmentVariable">
                                    <toolkit:DataRow HorizontalAlignment="Left">
                                        <TextBlock
                                            Margin="0,0,8,0"
                                            Text="{x:Bind Variable, Mode=OneWay}"
                                            ToolTipService.ToolTip="{x:Bind Variable, Mode=OneWay}" />
                                        <TextBlock
                                            Text="{x:Bind Value, Mode=OneWay}"
                                            TextTrimming="CharacterEllipsis"
                                            ToolTipService.ToolTip="{x:Bind Value, Mode=OneWay}" />
                                    </toolkit:DataRow>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                            <ListView.ItemContainerStyle>
                                <Style BasedOn="{StaticResource DefaultListViewItemStyle}" TargetType="ListViewItem">
                                    <Setter Property="HorizontalContentAlignment" Value="Left" />
                                </Style>
                            </ListView.ItemContainerStyle>
                        </ListView>
                    </ScrollViewer>
                </Grid>
                <CommandBar DefaultLabelPosition="Right">
                    <AppBarButton Command="{x:Bind CreateUserEnvVarCommand}" Label="New">
                        <AppBarButton.Icon>
                            <FontIcon Glyph="&#xE710;" />
                        </AppBarButton.Icon>
                    </AppBarButton>
                    <AppBarButton Command="{x:Bind EditUserEnvVarCommand}" Label="Edit">
                        <AppBarButton.Icon>
                            <FontIcon Glyph="&#xE70F;" />
                        </AppBarButton.Icon>
                    </AppBarButton>
                    <AppBarButton Command="{x:Bind DeleteUserEnvVarCommand}" Label="Delete">
                        <AppBarButton.Icon>
                            <FontIcon Glyph="&#xE74D;" />
                        </AppBarButton.Icon>
                    </AppBarButton>
                </CommandBar>
                <TextBlock FontWeight="SemiBold" Text="System variables" />
                <muxc:InfoBar
                    Title="These settings require administrator privileges to edit."
                    IsClosable="False"
                    IsOpen="True"
                    Visibility="{x:Bind ViewModel.IsAdmin, Mode=OneWay, Converter={StaticResource BoolToInverseVisibilityConverter}}">
                    <muxc:InfoBar.ActionButton>
                        <Button Command="{x:Bind RelaunchAsAdminCommand}" Style="{StaticResource AccentButtonStyle}">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <FontIcon FontSize="16" Glyph="&#xEA18;" />
                                <TextBlock Text="Relaunch as Administrator" />
                            </StackPanel>
                        </Button>
                    </muxc:InfoBar.ActionButton>
                </muxc:InfoBar>
                <Grid
                    Height="240"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="4">
                    <ScrollViewer Padding="4" IsEnabled="{x:Bind ViewModel.IsAdmin, Mode=OneWay}">
                        <ListView ItemsSource="{x:Bind ViewModel.SystemVariables}" SelectedIndex="{x:Bind ViewModel.SelectedSystemVariable, Mode=TwoWay}">
                            <ListView.Header>
                                <toolkit:DataTable Margin="12,4,0,8">
                                    <toolkit:DataColumn
                                        MinWidth="80"
                                        CanResize="True"
                                        Content="Name"
                                        DesiredWidth="100" />
                                    <toolkit:DataColumn
                                        MinWidth="160"
                                        CanResize="True"
                                        Content="Value"
                                        DesiredWidth="320" />
                                </toolkit:DataTable>
                            </ListView.Header>
                            <ListView.ItemTemplate>
                                <DataTemplate x:DataType="viewModels:EnvironmentVariable">
                                    <toolkit:DataRow HorizontalAlignment="Left">
                                        <TextBlock
                                            Margin="0,0,8,0"
                                            Text="{x:Bind Variable, Mode=OneWay}"
                                            ToolTipService.ToolTip="{x:Bind Variable, Mode=OneWay}" />
                                        <TextBlock
                                            Text="{x:Bind Value, Mode=OneWay}"
                                            TextTrimming="CharacterEllipsis"
                                            ToolTipService.ToolTip="{x:Bind Value, Mode=OneWay}" />
                                    </toolkit:DataRow>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                            <ListView.ItemContainerStyle>
                                <Style BasedOn="{StaticResource DefaultListViewItemStyle}" TargetType="ListViewItem">
                                    <Setter Property="HorizontalContentAlignment" Value="Left" />
                                </Style>
                            </ListView.ItemContainerStyle>
                        </ListView>
                    </ScrollViewer>
                </Grid>
                <CommandBar DefaultLabelPosition="Right" IsEnabled="{x:Bind ViewModel.IsAdmin, Mode=OneWay}">
                    <AppBarButton Command="{x:Bind CreateSystemEnvVarCommand}" Label="New">
                        <AppBarButton.Icon>
                            <FontIcon Glyph="&#xE710;" />
                        </AppBarButton.Icon>
                    </AppBarButton>
                    <AppBarButton Command="{x:Bind EditSystemEnvVarCommand}" Label="Edit">
                        <AppBarButton.Icon>
                            <FontIcon Glyph="&#xE70F;" />
                        </AppBarButton.Icon>
                    </AppBarButton>
                    <AppBarButton Command="{x:Bind DeleteSystemEnvVarCommand}" Label="Delete">
                        <AppBarButton.Icon>
                            <FontIcon Glyph="&#xE74D;" />
                        </AppBarButton.Icon>
                    </AppBarButton>
                </CommandBar>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
